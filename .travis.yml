language: c
before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq texinfo
install:
    - cd build-tools && bash build.sh && cd ..
cache:
    directories:
        - build-tools
script:
    - make aquila.iso
